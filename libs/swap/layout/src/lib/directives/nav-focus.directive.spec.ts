import { NavFocusDirective } from './nav-focus.directive';

describe('NavFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new NavFocusDirective();
    expect(directive).toBeTruthy();
  });
});
