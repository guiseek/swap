import { TabNextDirective } from './tab-next.directive';

describe('TabNextDirective', () => {
  it('should create an instance', () => {
    const directive = new TabNextDirective();
    expect(directive).toBeTruthy();
  });
});
